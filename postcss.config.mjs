import daisyui from "daisyui";

/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // daisyui: {},
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
